# This is a Databricks asset bundle definition for dab_retail.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_retail
  uuid: 4ecc73d5-47c0-45af-89a3-05e1957438b5

include:
  - resources/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-2137024003234611.11.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-2137024003234611.11.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/cxk04781@ucmo.edu to make sure we only have a single copy.
      root_path: /Workspace/Users/cxk04781@ucmo.edu/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: cxk04781@ucmo.edu
        level: CAN_MANAGE
